# ── Local development (.env) ───────────────────────────────────────────────
# Copy to .env and use with `docker compose up -d` + `npm run dev`
DATABASE_URL="mongodb://localhost:27017/tech_turf_tournament"

PAYSTACK_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY="pk_test_..."
ADMIN_PASSWORD="change-me"
ADMIN_SESSION_SECRET="random-secret-string"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
REGISTRATION_FEE="150000"

# ── Production (Vercel Environment Variables only — do NOT put in .env) ────────
# DATABASE_URL="mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/tech_turf_tournament?retryWrites=true&w=majority"
# NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
